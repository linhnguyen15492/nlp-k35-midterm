pymupdf
paddleocr
paddlepaddle
pytesseract
underthesea
sentence-transformers
google-generativeai   # optional: chỉ cần nếu dùng method auto_translate ở sentence_alignment
python-dotenv         # optional: chỉ cần nếu dùng method auto_translate ở sentence_alignment
pyyaml
tqdm
Pillow
# hanlp  # install separately: pip install hanlp  (~500MB model)
# Tesseract engine (cài riêng ngoài pip):
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki  (tích chọn "Vietnamese")
#   Linux:   sudo apt install tesseract-ocr tesseract-ocr-vie
#   macOS:   brew install tesseract && brew install tesseract-lang

